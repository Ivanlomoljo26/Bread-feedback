{
  "name": "miden-feedback-v2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute miden-feedback-v2-db --remote --file=./schema.sql",
    "db:local": "wrangler d1 execute miden-feedback-v2-db --local --file=./schema.sql",
    "test": "npm run check:migrations --silent && npm run check:schema --silent && vitest run",
    "test:watch": "vitest",
    "typecheck:test": "tsc --noEmit -p test/tsconfig.json",
    "check:schema": "python3 scripts/check-schema-drift.py submissions state_log issue_mirror dup_links sync_state store_reviews store_review_versions store_review_replies store_review_events store_sync_state",
    "check:migrations": "python3 scripts/validate-migrations.py"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260813.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260815.1": true
  }
}
